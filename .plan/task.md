# マインドマップアプリケーション タスクリスト

## 進行状況

**完了: 0/10** | **進行中: 0** | **未開始: 10**

---

## タスク一覧

### 1. 環境構築 - Viteプロジェクト初期化とReactFlowインストール
**優先度**: 🔴 最高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [x] package.jsonにreactflowが依存関係として追加されている
- [x] pnpmでインストール完了
- [x] `pnpm run dev` でプロジェクトが起動できる
- [x] 開発サーバーがブラウザで表示される

**実装ファイル**:
- `package.json`
- `pnpm-lock.yaml`

**参考**: 計画書の「環境構築」セクション

---

### 2. 型定義の準備 - TypeScript型定義ファイル作成
**優先度**: 🔴 最高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `src/types.ts` が作成されている
- [ ] `MindMapData` インターフェースが定義されている
- [ ] `CustomNodeData` インターフェースが定義されている
- [ ] ReactFlowから必要な型がインポートされている

**実装ファイル**:
- `src/types.ts` (新規作成)

**参考**: 計画書の「型定義の準備」セクション

---

### 3. localStorage操作ファイル作成 - 保存・読み込み・クリア機能
**優先度**: 🔴 最高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `src/storage.ts` が作成されている
- [ ] `saveToStorage()` 関数が実装されている
- [ ] `loadFromStorage()` 関数が実装されている
- [ ] `clearStorage()` 関数が実装されている
- [ ] すべての関数が正しく型定義されている

**実装ファイル**:
- `src/storage.ts` (新規作成)

**参考**: 計画書の「localStorage操作機能の実装」セクション

---

### 4. ReactFlow基本セットアップ - キャンバス表示とレイアウト
**優先度**: 🟠 高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `src/App.tsx` でReactFlowコンポーネントが実装されている
- [ ] 初期ノードが表示されている（ルートノード）
- [ ] `Controls` コンポーネントが表示されている
- [ ] `Background` コンポーネントが表示されている
- [ ] ズーム・パン・ドラッグが動作している
- [ ] ブラウザでキャンバスが正常に表示される

**実装ファイル**:
- `src/App.tsx` (既存を編集)
- `src/App.css` (必要に応じてスタイル追加)

**参考**: 計画書の「基本的なReactFlowコンポーネントの実装」セクション

---

### 5. ノード追加機能 - 新規ノード作成とボタン実装
**優先度**: 🟠 高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `handleAddNode()` コールバック関数が実装されている
- [ ] 新規ノードが正常に追加される
- [ ] 新規ノードに一意のIDが割り当てられている
- [ ] ノードの初期位置が画面内に設定されている
- [ ] UIに「ノード追加」ボタンが表示される

**実装ファイル**:
- `src/App.tsx`

**参考**: 計画書の「ノード操作機能の実装」セクション

---

### 6. ノード削除機能 - 選択ノード削除と関連エッジのクリーンアップ
**優先度**: 🟠 高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `handleDeleteNode()` コールバック関数が実装されている
- [ ] 選択ノードが削除される
- [ ] 削除ノードに関連するエッジがすべて削除される
- [ ] 孤立したエッジが残らない
- [ ] UIに「ノード削除」ボタンが表示される

**実装ファイル**:
- `src/App.tsx`

**参考**: 計画書の「ノード操作機能の実装」セクション

---

### 7. ノード間接続機能 - エッジ作成とノード接続ハンドラ
**優先度**: 🟠 高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `onConnect()` コールバック関数が実装されている
- [ ] ノード間をドラッグして接続できる
- [ ] エッジが正常に作成される
- [ ] 作成されたエッジが画面に表示される
- [ ] 接続の削除が可能である

**実装ファイル**:
- `src/App.tsx`

**参考**: 計画書の「ノード操作機能の実装」セクション

---

### 8. テキスト編集機能 - ノードラベルのインライン編集
**優先度**: 🟠 高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] ノードをダブルクリックで編集モードに変更できる
- [ ] テキスト入力フィールドが表示される
- [ ] 入力内容がノードラベルに反映される
- [ ] クリック外やEnterキーで編集終了できる
- [ ] 編集内容がstateに正しく反映される

**実装ファイル**:
- `src/components/CustomNode.tsx` (新規作成) または `src/App.tsx` (インライン編集)

**参考**: 計画書の「テキスト編集機能の実装」セクション

---

### 9. localStorage統合 - 保存・復元・自動保存の実装
**優先度**: 🟠 高 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] ページ読み込み時にlocalStorageからデータが復元される
- [ ] ノード・エッジ変更時に自動保存が動作する
- [ ] ブラウザをリロードしてもデータが保持される
- [ ] エラーハンドリングが実装されている
- [ ] `handleClearData()` でデータクリアが機能する

**実装ファイル**:
- `src/App.tsx` (useEffect追加)

**参考**: 計画書の「データ永続化の統合」セクション

---

### 10. UIツールバー実装 - コンポーネント化とスタイリング
**優先度**: 🟡 中 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `src/components/Toolbar.tsx` が作成されている
- [ ] 「ノード追加」「ノード削除」「データクリア」ボタンがある
- [ ] ツールバーが画面左上に固定表示される
- [ ] ボタンのスタイルが統一されている
- [ ] マウスホバーで視覚的フィードバックがある
- [ ] ReactFlowキャンバスの上に正しく表示される (z-index)

**実装ファイル**:
- `src/components/Toolbar.tsx` (新規作成)
- `src/App.tsx` (Toolbarコンポーネントをインポート)

**参考**: 計画書の「UIの整備」セクション

---

## オプション機能 (実装は優先度低)

### 11. エクスポート・インポート機能 - JSON形式でのデータ操作
**優先度**: 🟢 低 | **ステータス**: ⏳ 未開始

**完了基準**:
- [ ] `handleExport()` でマインドマップをJSONファイルとしてダウンロードできる
- [ ] `handleImport()` でJSONファイルをアップロード・読み込みできる
- [ ] インポート時のバリデーション処理がある
- [ ] エラーハンドリングが実装されている

**実装ファイル**:
- `src/App.tsx`

**参考**: 計画書の「オプション機能の追加」セクション

---

## テスト・デバッグ項目

完了後の確認項目:
- [ ] ノードの追加・削除が正常に動作
- [ ] エッジの接続が正しく行われる
- [ ] テキスト編集が機能する
- [ ] ドラッグ、ズーム、パンが動作する
- [ ] localStorageへの保存・読み込みが正常
- [ ] ブラウザリロード後にデータが保持される
- [ ] エッジケース対応（ノード0個の時など）

**参考**: 計画書の「テストとデバッグ」セクション

---

## ファイル構成の最終状態

```
/app/
├── src/
│   ├── App.tsx              # メインコンポーネント
│   ├── App.css              # スタイル
│   ├── main.tsx             # エントリーポイント
│   ├── index.css            # グローバルスタイル
│   ├── types.ts             # ✅ 型定義
│   ├── storage.ts           # ✅ localStorage操作
│   ├── components/
│   │   ├── Toolbar.tsx      # ✅ ツールバー
│   │   └── CustomNode.tsx   # (オプション)
│   └── assets/
├── package.json
├── pnpm-lock.yaml
├── tsconfig.json
├── vite.config.ts
├── index.html
└── .plan/
    ├── plan.md
    └── task.md              # このファイル
```

---

## 注記

- 各タスクの完了基準をすべて満たす必要があります
- タスクは上から順序に進めることを推奨します
- 各タスク完了後、ブラウザでの動作確認が必須です
- 不具合発生時は、ブラウザの開発者ツール (DevTools) でエラーを確認してください
